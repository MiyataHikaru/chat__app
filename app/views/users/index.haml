.user-index-wrapper
  %h2 ユーザー一覧
  .index-user-list
    - @users.each do |user|
      = link_to user_path(user) do
        .user-box
          = user_image(user)
          .user-name
            = user.name
          .user-content
            = user.content
    .clear
  .pagenate-box
    = will_paginate
